


// Instantiate the module
data_MEM instance_name (
    .address(address), 
    .writeData(writeData), 
    .readDataMem(readDataMem), 
    .memWrite(memWrite), 
    .memRead(memRead), 
    .clk(clk)
    );


